import React, { ReactElement } from 'react';

export const HomePage = (): ReactElement => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <h1>Home</h1>
    </div>
  );
};
