import React, { ReactElement } from 'react';

export const MenuPage = (): ReactElement => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <h1>Menu</h1>
    </div>
  );
};
