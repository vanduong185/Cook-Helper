import React, { ReactElement } from 'react';

export const ItemManager = (): ReactElement => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <h1>ItemManager</h1>
    </div>
  );
};
